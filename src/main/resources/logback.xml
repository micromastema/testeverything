<configuration>
<!--    &lt;!&ndash;为了防止进程退出时，内存中的数据丢失，请加上此选项&ndash;&gt;-->
<!--    <shutdownHook class="ch.qos.logback.core.hook.DelayingShutdownHook"/>-->

    <appender name="loghubAppender" class="com.aliyun.openservices.log.logback.LoghubAppender">
        <!--必选项-->
        <!-- 账号及网络配置 -->
        <endpoint>cn-beijing.log.aliyuncs.com</endpoint>
        <accessKeyId>LTAI4Ffsd7EspeKbiNUk3p2S</accessKeyId>
        <accessKeySecret>A4uMfT0c38Cru2ttWMPF2qSGNhekYu</accessKeySecret>

        <!-- sls 项目配置 -->
        <project>ivykid-test</project>
        <logStore>test1</logStore>

        <!-- 可选项 -->
        <topic>topic3</topic>
        <source>source3</source>

        <!-- 可选项 详见 '参数说明'-->
<!--        <totalSizeInBytes>104857600</totalSizeInBytes>-->
<!--        <maxBlockMs>60000</maxBlockMs>-->
<!--        <ioThreadCount>8</ioThreadCount>-->
<!--        <batchSizeThresholdInBytes>524288</batchSizeThresholdInBytes>-->
<!--        <batchCountThreshold>4096</batchCountThreshold>-->
<!--        <lingerMs>2000</lingerMs>-->
<!--        <retries>10</retries>-->
<!--        <baseRetryBackoffMs>100</baseRetryBackoffMs>-->
<!--        <maxRetryBackoffMs>100</maxRetryBackoffMs>-->

        <!-- 可选项 通过配置 encoder 的 pattern 自定义 log 的格式 -->
        <encoder>
            <pattern>%d %-5level [%thread] %logger{0}: %msg</pattern>
        </encoder>

        <!-- 可选项 设置时间格式 -->
        <timeFormat>yyyy-MM-dd HH:mm:ss</timeFormat>
        <!-- 可选项 设置时区 -->
        <timeZone>Asia/Shanghai</timeZone>

        <filter class="ch.qos.logback.classic.filter.LevelFilter"><!-- 只打印INFO级别的日志 -->
            <level>INFO</level>
        </filter>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger - %msg %X{THREAD_ID} %n</pattern>
        </encoder>
    </appender>

    <root>
        <level value="INFO"/>
        <appender-ref ref="loghubAppender"/>
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>